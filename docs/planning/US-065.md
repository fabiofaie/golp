# Piano US-065 — Correzione mockup dashboard-v2 — rimozione CTA duplicato in header

**Generato il:** 2026-07-11 | **Status storia:** PLANNED

## Riferimenti
- Storia in: `docs/BACKLOG.md` §US-065 (riga 2253)
- Mockup: `docs/mockups/dashboard-v2/index.html`

---

## Analisi requisiti

Ispezionando `docs/mockups/dashboard-v2/index.html` (77 righe, letto per intero data la dimensione ridotta), il CTA duplicato è alla riga 22: `<a class="primary compact" href="#">＋ Partita</a>` dentro la sezione `.welcome`, subito sotto l'header — è l'elemento che l'AC descrive come "CTA duplicato in header" (visivamente adiacente all'header, anche se non dentro il tag `<header>` in senso stretto). Il bottone centrale della bottom-nav (riga 70, `<a class="nav-action" href="#"><span>＋</span></a>`) esiste già ma **non ha l'etichetta "Registra"** richiesta esplicitamente da AC2 — oggi mostra solo l'icona, diversamente dagli altri item della bottom-nav che hanno tutti icona+testo. Nessun'altra sezione del mockup (attention, performance, ultime partite) contiene un secondo invito a registrare partite — AC3 è già soddisfatto, nessuna modifica necessaria lì. Nessuna ambiguità critica.

## Soluzione tecnica

Approccio: rimuovere il link "＋ Partita" dalla sezione `.welcome`, aggiungere il testo "Registra" al bottone centrale della bottom-nav per allinearlo agli altri item. Nessuna modifica di layout oltre questi due punti (out of scope esplicito). File toccato: solo `docs/mockups/dashboard-v2/index.html` — nessun impatto su `src/` (il mockup è statico e di riferimento, non collegato al codice applicativo).

**Componenti toccati:**

| Componente | Modifica |
|---|---|
| `docs/mockups/dashboard-v2/index.html` | Rimuovi riga 22 (`<a class="primary compact">＋ Partita</a>`); aggiungi testo "Registra" dentro `.nav-action` (riga 70) |

**Modifiche al modello dati:** nessuna.
**Contratti API:** nessuno.
**Rischi:** nessuno — modifica isolata a un file mockup statico, nessun blocker (`Blocked by: -`).

## Task

- **TASK-1** ✅ (2026-07-11 09:00): Rimuovi `<a class="primary compact" href="#">＋ Partita</a>` (riga 22) dalla sezione `.welcome` in `docs/mockups/dashboard-v2/index.html`.
  File: `docs/mockups/dashboard-v2/index.html`. Acceptance: copre AC1. Verifica: ispezione visiva (apri il file in browser).

- **TASK-2** ✅ (2026-07-11 09:00): Aggiungi il testo "Registra" dentro `<a class="nav-action">` (riga 70), coerente con gli altri item della bottom-nav che hanno icona+testo.
  File: `docs/mockups/dashboard-v2/index.html`. Acceptance: copre AC2. Verifica: ispezione visiva.

- **TASK-3** ✅ (2026-07-11 09:01): Verifica finale: apri il mockup nel browser, conferma che l'unico controllo per registrare una partita sia il "+" centrale della bottom-nav con etichetta "Registra", e che nessun'altra sezione (welcome, attention, performance, ultime partite) contenga un secondo CTA equivalente.
  File: nessuno (verifica). Acceptance: copre AC1, AC2, AC3 congiuntamente. Verifica: manuale, screenshot opzionale.

## Test strategy

| AC | Verifica |
|---|---|
| AC1 (CTA rimosso da header/welcome) | TASK-1 + TASK-3 ispezione visiva |
| AC2 (bottom-nav "+" con etichetta "Registra") | TASK-2 + TASK-3 ispezione visiva |
| AC3 (nessun secondo CTA altrove) | TASK-3 ispezione visiva dell'intero mockup |

**Strategia e2e:** non applicabile — mockup statico, nessun comportamento eseguibile da testare automaticamente.

**Demo video:** no — modifica di 2 righe HTML su un file di riferimento statico, verificabile con uno sguardo. Non giustifica alcun artefatto oltre a uno screenshot opzionale in fase di review umana.

## Rischi aperti
Nessuno. Nessun blocker (`Blocked by: -`).

---

> **PROSSIMO PASSO:** esegui `/eq-implement US-065` direttamente. Nessun mockup nuovo da generare — è la correzione stessa del mockup esistente.
